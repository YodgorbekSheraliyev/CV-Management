services:
  backend:
    image: ${DOCKER_REGISTRY-}backend
    build:
      context: backend
      dockerfile: Dockerfile
  frontend:
    image: ${DOCKER_REGISTRY-}frontend
    build:
      context: frontend
      dockerfile: Dockerfile
